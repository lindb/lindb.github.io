"use strict";(self.webpackChunklindb_doc=self.webpackChunklindb_doc||[]).push([[8863],{5744:(e,o,t)=>{t.r(o),t.d(o,{default:()=>C});var a=t(6252),i=t(5806),d=t(3547),c=t(7404),l=t(227),n=t(4565);const s=(0,a._)("h1",{id:"storage",tabindex:"-1"},[(0,a._)("a",{class:"header-anchor",href:"#storage","aria-hidden":"true"},"#"),(0,a.Uk)(" Storage")],-1),r=(0,a._)("p",null,[(0,a.Uk)("All data of "),(0,a._)("code",null,"LinDB"),(0,a.Uk)(" will be stored on the local disk, and there are different storage structures according to different data types:")],-1),h=(0,a._)("li",null,[(0,a._)("code",null,"Metirc Metadata"),(0,a.Uk)(": store "),(0,a._)("code",null,"Metric Name"),(0,a.Uk)(" and its underlying "),(0,a._)("code",null,"Fields"),(0,a.Uk)("/"),(0,a._)("code",null,"Tag Keys"),(0,a.Uk)(";")],-1),u=(0,a._)("code",null,"Tags(Series) Index",-1),p=(0,a._)("code",null,"Tags",-1),f=(0,a._)("code",null,"Metric",-1),m=(0,a._)("code",null,"2",-1),g=(0,a._)("li",null,[(0,a.Uk)("Forward: "),(0,a._)("code",null,"Tags"),(0,a.Uk)(" corresponding to "),(0,a._)("code",null,"Series ID"),(0,a.Uk)(";")],-1),y=(0,a._)("code",null,"Tag Key/Value",-1),b=(0,a._)("code",null,"Series ID",-1),k=(0,a._)("code",null,"Tag Value",-1),v=(0,a._)("code",null,"Series ID",-1),w={href:"http://roaringbitmap.org",target:"_blank",rel:"noopener noreferrer"},_=(0,a._)("li",null,[(0,a._)("code",null,"Data"),(0,a.Uk)(": Storage of "),(0,a._)("code",null,"Data Point"),(0,a.Uk)(" under all "),(0,a._)("code",null,"Time Series"),(0,a.Uk)(";")],-1),T=(0,a.uE)('<p>All the above data types are stored in a common underlying <code>KV Store</code>.</p><h2 id="time-series-characteristics" tabindex="-1"><a class="header-anchor" href="#time-series-characteristics" aria-hidden="true">#</a> Time series characteristics</h2><p>Before talking about storage, let&#39;s talk about the characteristics of time-series, as shown in the figure:</p><p><img src="'+i+'" alt="time series characteristic"></p><p>Time series data characteristics (according to its time characteristics, it can be divided into time-invariant and time-variant data)</p><ol><li><code>Time Series</code> =&gt; <code>Metric + Tags</code>: This part of the data is basically a string, and this data occupies the bulk of the data packet, but it will not vary with time flows. Converting these strings into numerical values for storage to reduce storage costs;</li><li><code>Fields</code>: This part of the data is basically numeric and changes with time, but the numeric type is easy to compress;</li></ol><h2 id="storage-structure" tabindex="-1"><a class="header-anchor" href="#storage-structure" aria-hidden="true">#</a> Storage structure</h2><h3 id="database" tabindex="-1"><a class="header-anchor" href="#database" aria-hidden="true">#</a> Database</h3><p><img src="'+d+'" alt="storage database"></p>',9),S=(0,a._)("li",null,[(0,a.Uk)("The data of a database are distributed on different nodes of the "),(0,a._)("code",null,"Storage"),(0,a.Uk)(" cluster according to "),(0,a._)("code",null,"Shard"),(0,a.Uk)(";")],-1),x=(0,a._)("code",null,"Shard",-1),U=(0,a.uE)('<h3 id="shard" tabindex="-1"><a class="header-anchor" href="#shard" aria-hidden="true">#</a> Shard</h3><div class="language-markdown" data-ext="md"><pre class="language-markdown"><code>├─ db_test_1\n|  ├─ meta\n|  |  ├─ namespace\n|  |  ├─ metric\n|  |  ├─ field\n|  |  ├─ tagkey\n|  |  └─ tagvalue\n|  └─ shard\n|     ├─ 1\n|     |  ├─ inverted\n|     |  ├─ forward\n|     |  └─ segment\n|     |     ├─ day\n|     |     |  ├─ 20190101\n|     |     |  |  ├─ 01\n|     |     |  |  ├─ 02\n|     |     |  |  └─ 23\n|     |     |  └─ 20190102\n|     |     ├─ month\n|     |     |  ├─ 201901\n|     |     |  |  ├─ 01\n|     |     |  |  ├─ 02\n|     |     |  |  └─ 31\n|     |     |  └─ 201902\n|     |     └─ year\n|     |        ├─ 2019\n|     |        |  ├─ 01\n|     |        |  ├─ 02\n|     |        |  └─ 12\n|     |        └─ 2020\n|     └─ 2\n└─ db_test_2\n</code></pre></div><p>The storage directory above describes how data are stored on a <code>Storage</code> node.</p><p>Taking the data structure of a single database on a single node as an example:</p><ul><li><code>meta</code>: store all <code>metadata</code>, at database level: <ul><li><code>namespace</code></li><li><code>metric</code></li><li><code>field</code></li><li><code>tag key</code></li><li><code>tag value</code></li></ul></li><li><code>shard</code>: A database will have multiple <code>shard</code>s on a single node, and each <code>shard</code> has the following data: <ul><li><code>index</code>: store forward and reverse indexes;</li><li><code>segment</code>: store the data of each time slice;</li></ul></li></ul>',5),B=(0,a._)("code",null,"meta",-1),I=(0,a._)("code",null,"index",-1),V=(0,a.uE)('<p>All data are divided into different parts by the <code>Interval</code> of the database to store specific data in time slices:</p><ul><li>The time series has strong time correlation, which makes it easier for querying;</li><li>Convenient for handling <code>TTL</code>. If the data expires, just delete the corresponding directory directly;</li><li>There are multiple <code>segments</code> under each <code>shard</code>, and each <code>segment</code> stores the data of the corresponding time slice according to the corresponding <code>interval</code>;</li><li>Why are there many <code>data families</code> stored according to <code>interval</code> under each <code>segment</code>? This is mainly because the main problem solved by <code>LinDB</code> is to store a large amount of monitoring data. The general monitoring data is basically written at the latest time, and basically no historical data will be written. The data storage of the entire <code>LinDB</code> is similar to the <code>LSM</code> method, in order to reduce the merge operation between data files which leads to write amplification. then the re-sharding policy of <code>segment</code> time slice is adopted.</li></ul><p>The following is an example of <code>interval</code> being <code>10s</code>:</p><ol><li><code>segment</code> is stored by day;</li><li>Each <code>segment</code> is divided into <code>data family</code> by hour, one <code>family</code> per hour, and the files in each <code>family</code> store specific data in columns;</li></ol><h2 id="kv-store" tabindex="-1"><a class="header-anchor" href="#kv-store" aria-hidden="true">#</a> KV store</h2><div class="language-markdown" data-ext="md"><pre class="language-markdown"><code>└─ kv_store_1\n   ├─ CURRENT\n   ├─ LOCK\n   ├─ MANIFEST-000010\n   ├─ OPTIONS\n   ├─ family_1\n   |  ├─ 000001.sst\n   |  ├─ 000002.sst\n   |  ├─ 000004.sst\n   |  └─ 000008.sst\n   ├─ family_2\n   |  ├─ 000011.sst\n   |  ├─ 000012.sst\n   |  ├─ 000014.sst\n   |  └─ 000018.sst\n   └─ family_3\n</code></pre></div><p>The whole <code>KV Store</code> is similar to <code>LSM</code>, but it is different from <code>LSM</code>. The main differences are as follows:</p><ul><li>There is no <code>Memory Table</code>, because the whole system has a <code>Memory Database</code> to store all recently data, and these data will be directly written to the <code>KV Store</code> later;</li><li><code>Key</code>&#39;s type is <code>uint32</code>, because all <code>string</code> will be converted to <code>uint32</code> according to the timing characteristics, so the underlying <code>KV Store</code> is directly designed as an <code>uint32 =&gt; binary</code> structure;</li></ul><p>The directory structure is very similar to <code>rocksdb</code> and supports <code>column family</code>.</p><ul><li><code>CURRENT</code>: record the currently valid <code>MANIFEST</code> file;</li><li><code>LOCK</code>: file lock to prevent multiple processes from opening the same <code>KV Store</code>;</li><li><code>MANIFEST</code>: <code>change log</code> of all <code>sstable</code> changes, including some <code>change log</code> of <code>sequence</code>, etc.;</li><li><code>OPTIONS</code>: <code>KV Store</code> configuration information, including configuration information at each <code>column family</code> level;</li><li><code>KV Store</code> can store multiple <code>column families</code>, and each <code>family</code> stores multiple <code>sstable</code> files;</li></ul><h3 id="compaction" tabindex="-1"><a class="header-anchor" href="#compaction" aria-hidden="true">#</a> Compaction</h3><p><img src="'+c+'" alt="storage compaction"></p><ul><li>Each <code>KV Store</code> will start a <code>Goroutine</code> periodically to <code>Check</code> whether there are too many files in each <code>Family Level 0</code> and meet the conditions of <code>Compaction</code>;</li><li>When the conditions are met, the corresponding <code>Family</code> will be notified to execute <code>Compaction Job</code>, if another <code>Compaction</code> job is being executed, this operation will be ignored, all operations will only be done in one <code>Goroutine</code>,of which the benefit is lock-free , because <code>Compaction Job</code> is a very heavy operation, and if locking is required, it may affect the writing of new files</li></ul><p><code>Compaction</code> mainly merges the files in <code>Level 0</code> into <code>Level 1</code>. Currently, <code>LinDB</code> has only 2 Level, which is different from <code>LevelDB</code>.</p><p><code>Compaction</code> condition: any one of the following conditions will trigger the Compact task:</p><ul><li>the number of files in <code>Level 0</code> exceeds the number of files configured;</li></ul><p>There are 2 merge process:</p><ul><li>Directly moving the files of <code>Level 0</code> to <code>Level 1</code>, but the conditions of <code>compact</code> should be met: the number of files of <code>pick files from level 0</code> is <code>1</code>, the number of files of <code>pick fiels from level 1</code> is <code>1</code> The number of files is <code>0</code>, that is, only one file needs to be merged, just modify <code>Metadata</code>;</li><li>Need to combine multiple files into one large file;</li></ul><p>The <code>Compact</code> process is as follows:</p><ol><li>Select the file that needs <code>Compact</code>, firstly select the current <code>level 0</code> file, then traverse each <code>level 0</code> file, press <code>key range</code> to get the overlapping file from <code>level 1</code>, why is here? Fetch <code>level 1</code> files by each <code>level 0</code> file, not by the final <code>key range</code> in <code>level 0</code>. Because the <code>keys</code> of the entire <code>level 0</code> file may be relatively hashed, so if we take the final <code>range</code>, we may get a large range. E.g:</li></ol><div class="language-yaml" data-ext="yml"><pre class="language-yaml"><code><span class="token key atrule">for example</span><span class="token punctuation">:</span>\n<span class="token key atrule">Level 0</span><span class="token punctuation">:</span>\n    <span class="token key atrule">file 1</span><span class="token punctuation">:</span> 1~10\n    <span class="token key atrule">file 2</span><span class="token punctuation">:</span> 1000~1001\n    \n<span class="token key atrule">Level 1</span><span class="token punctuation">:</span>\n    <span class="token key atrule">file 3</span><span class="token punctuation">:</span> 1~5\n    <span class="token key atrule">file 4</span><span class="token punctuation">:</span> 100~200\n    <span class="token key atrule">file 5</span><span class="token punctuation">:</span> 400~500\n    \n<span class="token punctuation">-</span> If we press the final range of level 0<span class="token punctuation">,</span> 1~1001<span class="token punctuation">,</span> this will take out all the files of level 1\n<span class="token punctuation">-</span> If we take it according to the range of each level 0<span class="token punctuation">,</span> we only need to take the file 3 (1~5) in the end.\n</code></pre></div><ol start="2"><li>The entire <code>compact</code> process is actually a multi-way merge process. Since the <code>key</code> is sorted when <code>flush</code> writes files, <code>compact</code> only needs to read each file in order, and press <code> The order of key</code> can traverse the data from these files;</li></ol><ul><li>During the process, the data with the same <code>key</code> needs to be merged. The merging process needs to be merged according to different data types, and the <code>Merger Interface</code> needs to be implemented;</li><li>If <code>key</code> does not need the merge operation, write the corresponding data directly to the file, which can reduce unnecessary serialization operations;</li></ul><ol start="3"><li>Once <code>compact</code> merging and writing files is successfully completed, submitting <code>Version Edit</code> to <code>Version Set</code> will be taken. At this time, <code>Version Edit</code> includes newly written files and old files that need to be deleted. This process requires locks;</li><li>Delete those useless files: such as files that have been merged, or some intermediate result files after failure. It should be noted here that the cleanup operation must be done after the <code>Version Edit</code> is successfully submitted to the <code>Version Set</code>, otherwise which may leads to data file confusion;</li></ol><div class="custom-container tip"><p class="custom-container-title">TIP</p><ul><li>Version Edit: Similar to LevelDB, a Version Edit will be recorded for each file write operation, and Version Edit will record new files/deleted files, so that when the system restarts or crashes, just reload the Version Edit Log again. There are those that mention the whole useful document;</li><li>Version Set: record which files are currently stored and available;</li></ul></div><h3 id="rollup" tabindex="-1"><a class="header-anchor" href="#rollup" aria-hidden="true">#</a> Rollup</h3><p><img src="'+l+'" alt="storage sstable"></p><p><code>Rollup Job</code> is a special <code>Compact Job</code>, which mainly deals with data reduction (<code>DownSampling</code>), namely <code>10s-&gt;5m-&gt;1h</code>, its core logic is same as <code>Compaction Job</code>, the main differences are as follows :</p><ol><li><code>Source Family</code> merges data into <code>Target Family</code> to operate <code>2</code> <code>Family</code>;</li><li>After the merging is completed, delete the files that need <code>Rollup</code> in <code>Source Family Version</code>, and record the files that have been <code>Rollup</code> in <code>Target Family Version</code> to prevent duplicate data merging;</li></ol><h3 id="sstable-layout" tabindex="-1"><a class="header-anchor" href="#sstable-layout" aria-hidden="true">#</a> SSTable layout</h3><p><img src="'+n+'" alt="storage sstable"></p><p>The structure of each <code>SSTable</code> is shown in the figure above, and it mainly has the following components:</p><ul><li><code>Footer Block</code>: Storing <code>Magic Number(8 Bytes) + Version(1 byte) + Index Block Offset(4 bytes) + Offset Block Offset(4 bytes)</code>, which can be accessed through both <code>Index Block Offset</code> and <code>Offset Block</code> to read contents of <code>Index Block</code> and <code>Offset Block</code>;</li><li><code>Index Block</code>: Using <code>Roaring Bitmap</code> to store all the <code>Keys</code> in the current <code>SSTable</code> file, as all <code>Key</code> are <code>uint32</code>, so we can directly use <code>Roaring Bitmap</code> to store, such benefits is that we can use <code>Roaring Bitmap</code> to determine whether a <code>Key</code> exists, and at the same time we can also know the position of this <code>Key</code> in this <code>Roaring Bitmap</code>;</li><li><code>Offset Block</code>: store all the <code>Offset</code> of <code>Value Entry</code>, and each <code>Offset</code> is stored with a fixed length, so if it is found in the <code>Nth</code> position in the <code>Index Block</code>, The <code>Offset</code> of that <code>Value Entry</code> is the data pointed to by <code>N * Offset Length</code>;</li><li><code>Value Entry</code>: store the <code>Value</code> corresponding to each <code>Key/Value</code>, because the <code>Key</code> is already stored in the <code>Index Block</code>, so the <code>Value Entry</code> only needs to store the <code>Value</code> in the <code>Key/Value</code>;</li></ul><p>The advantage of this is that the compression of <code>Key</code> can be done well, and <code>Roaring Bitmap</code> has done a lot of optimization on <code>Bitmap</code>, <code>Get</code> data through <code>Key</code> is very efficient, because it is not like <code>LevelDB</code>, binary searching <code>Key</code> in the middle process are not required, and the <code>Bitmap</code> can be resident in memory.</p><p>Based on the above storage structure, the entire query logic is as follows:</p><ol><li>Through <code>Index Block</code>, we can directly know whether the queried <code>Key</code> exists. If it does not exist, return it directly. If it exists, get the first position (<code>Index</code>) in the <code>Bitmap</code>;</li><li>First jump: According to the <code>Index</code> obtained above, in <code>Offset Block</code>, after skipping <code>Index * Offset Length</code>, we can get <code>Value Entry Offset(Position)</code>;</li><li>The second jump: According to the <code>Position</code> obtained above, skip the <code>Position</code> at the beginning of the file and then the desired <code>Value</code> can be read directly;</li></ol><p>If we want to do the <code>Scan</code> operation, just directly read the sequence based on the <code>Index Block</code> and <code>Offset Blcok</code>.</p><h6 id="reference" tabindex="-1"><a class="header-anchor" href="#reference" aria-hidden="true">#</a> Reference</h6>',38),L={href:"http://roaringbitmap.org",target:"_blank",rel:"noopener noreferrer"},K={href:"http://opentsdb.net/docs/build/html/user_guide/uids.html",target:"_blank",rel:"noopener noreferrer"},E={href:"https://rocksdb.org/",target:"_blank",rel:"noopener noreferrer"},O={},C=(0,t(3744).Z)(O,[["render",function(e,o){const t=(0,a.up)("ExternalLinkIcon"),i=(0,a.up)("RouterLink");return(0,a.wg)(),(0,a.iD)("div",null,[s,r,(0,a._)("ul",null,[h,(0,a._)("li",null,[u,(0,a.Uk)(": store all "),p,(0,a.Uk)(" under a "),f,(0,a.Uk)(", this part is divided into "),m,(0,a.Uk)(" data types; "),(0,a._)("ul",null,[g,(0,a._)("li",null,[(0,a.Uk)("Inverted: The inverted index of "),y,(0,a.Uk)(", where "),b,(0,a.Uk)(" are binding to "),k,(0,a.Uk)(". "),v,(0,a.Uk)(" set is compressed stored by "),(0,a._)("a",w,[(0,a.Uk)("RoaringBitMap"),(0,a.Wm)(t)]),(0,a.Uk)(" ;")])])]),_]),T,(0,a._)("ul",null,[S,(0,a._)("li",null,[(0,a.Uk)("A "),x,(0,a.Uk)(" can have multiple replicas, please see "),(0,a.Wm)(i,{to:"/design/replication.html"},{default:(0,a.w5)((()=>[(0,a.Uk)("Replication")])),_:1}),(0,a.Uk)(" for details;")])]),U,(0,a._)("p",null,[(0,a.Uk)("See "),(0,a.Wm)(i,{to:"/design/index_.html"},{default:(0,a.w5)((()=>[(0,a.Uk)("Index")])),_:1}),(0,a.Uk)(" for the structure of "),B,(0,a.Uk)(" and "),I,(0,a.Uk)(".")]),V,(0,a._)("ol",null,[(0,a._)("li",null,[(0,a._)("a",L,[(0,a.Uk)("RoaringBitMap"),(0,a.Wm)(t)])]),(0,a._)("li",null,[(0,a._)("a",K,[(0,a.Uk)("OpenTSDB UID"),(0,a.Wm)(t)])]),(0,a._)("li",null,[(0,a._)("a",E,[(0,a.Uk)("RocksDB"),(0,a.Wm)(t)])])])])}]])},7404:(e,o,t)=>{e.exports=t.p+"assets/img/storage_compaction.34dc977d.png"},3547:(e,o,t)=>{e.exports=t.p+"assets/img/storage_database.1c521be9.png"},227:(e,o,t)=>{e.exports=t.p+"assets/img/storage_rollup.45abc25c.png"},4565:(e,o,t)=>{e.exports=t.p+"assets/img/storage_sstable.df1e7203.png"},5806:(e,o,t)=>{e.exports=t.p+"assets/img/time_series_characteristic.e42b734d.png"},6828:(e,o,t)=>{t.r(o),t.d(o,{data:()=>a});const a=JSON.parse('{"key":"v-1b654f54","path":"/design/storage.html","title":"Storage","lang":"en-US","frontmatter":{},"excerpt":"","headers":[{"level":2,"title":"Time series characteristics","slug":"time-series-characteristics","link":"#time-series-characteristics","children":[]},{"level":2,"title":"Storage structure","slug":"storage-structure","link":"#storage-structure","children":[{"level":3,"title":"Database","slug":"database","link":"#database","children":[]},{"level":3,"title":"Shard","slug":"shard","link":"#shard","children":[]}]},{"level":2,"title":"KV store","slug":"kv-store","link":"#kv-store","children":[{"level":3,"title":"Compaction","slug":"compaction","link":"#compaction","children":[]},{"level":3,"title":"Rollup","slug":"rollup","link":"#rollup","children":[]},{"level":3,"title":"SSTable layout","slug":"sstable-layout","link":"#sstable-layout","children":[]}]}],"git":{"updatedTime":1681286958000,"contributors":[{"name":"stone1100","email":"stonejh1100@gmail.com","commits":1}]},"filePathRelative":"design/storage.md"}')}}]);