import{_ as e,a as o,b as t}from"./cross_idc_query.7afaf5b6.js";import{_ as r,b as a}from"./app.1245c310.js";const i={},n=a('<h1 id="query" tabindex="-1"><a class="header-anchor" href="#query" aria-hidden="true">#</a> Query</h1><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview" aria-hidden="true">#</a> Overview</h2><p>The roles involved in the query are as follows:</p><ul><li><code>Broker</code>: Receive the user&#39;s query, generate the corresponding execution plan according to the query statement, and send it to the corresponding <code>Storage</code> node, and aggregate the results returned by each <code>Storage</code> node to generate the final result;</li><li><code>Storage</code>: perform data filtering, <code>Downsampling</code> and the simplest atomic calculation (that is, there is a certain ability to push down operators);</li></ul><p>The entire query sequence is as follows, here it does not matter whether it is executed on <code>Broker</code> or <code>Storage</code>, just look at the entire query process:</p><ol><li>Query Language Plan</li><li>Filtering</li><li>Scan Time Series</li><li>Grouping if need</li><li>DownSampling</li><li>Aggregation</li><li>Functions</li><li>Expressions</li></ol><p>Since <code>LinDB</code> is implemented in <code>Golang</code>, an asynchronous operation and high concurrency can be well supported through <code>Goroutine</code>, but the system still uses the concept of <code>Goroutine Pool</code> to better manage and handle these asynchronous operations <code>Task</code>.</p><p>The entire query process is completed asynchronously and works in an asynchronous <code>Task</code> way, that is, <code>Task A</code> only does one thing, the result of <code>Task A</code> may be the <code>Input</code> of <code>Task B</code>, but <code>Task B</code> It will not wait for the arrival of the result of <code>Task A</code>, but <code>Trigger Task B</code> in the way of <code>Event</code>, and finally complete the execution of a query <code>Pipeline</code>.</p><p>It should be noted here that the system will not open several <code>Goroutine</code> to complete a request, but complete it in different <code>Goroutine Pool</code>, all <code>Task</code> have no return value, and <code>Event Driver</code> way of processing.</p><p>Let&#39;s take an example to illustrate. For example, <code>Scan Goroutine Pool</code> only performs <code>Scan</code> operation, instead of starting a <code>Goroutine Scan</code> data, waiting for the next <code>Task</code> result and returning it to the upper <code>Task</code> for merging, In this way, each <code>Goroutine</code> will still have waiting behavior during the whole processing process, but the result of <code>Scan</code> is directly sent to the next <code>Task</code>, and the data is merged with a special <code>Task</code> to complete, the advantage of this is the <code>Scan Goroutine</code> <code>After completing a </code>Scan<code>operation, you can do the next</code>Scan` operation, which can make full use of system resources.</p><p>Try to use <code>Streaming</code> to complete the entire query process to reduce the <code>GC</code> pressure caused by unnecessary object creation, and also <code>Pool</code> some reusable and high-frequency objects to improve memory. usage rate.</p><p>According to different query conditions, you can <code>Plan</code> the following types of execution plans:</p><ul><li><code>Simple Query</code>: simple aggregation query;</li><li><code>Complex Query</code>: query with <code>Grouping</code>;</li><li><code>Cross IDC Query</code>: query across <code>IDC</code>;</li></ul><h2 id="simple-query" tabindex="-1"><a class="header-anchor" href="#simple-query" aria-hidden="true">#</a> Simple query</h2><p><img src="'+e+'" alt="simple_query"></p><ol><li>The <code>Broker</code> node that receives the user request is used as the <code>Root</code> node, executes the <code>Plan</code> operation according to the current <code>Database</code> status to be queried, generates an execution plan, and sends the query request to the <code>Storage</code> cluster related The node (<code>Leaf</code> node) executes;</li><li>The <code>Leaf</code> node performs operations such as <code>Filtering=&gt;Scan=&gt;Downsampling=&gt;Aggregation</code> according to the query conditions;</li><li>Since there is no <code>Grouping</code> operation, the final result can be directly returned to the <code>Root</code> node for the final aggregation operation, and the final result is returned to the user;</li></ol><h2 id="complex-query" tabindex="-1"><a class="header-anchor" href="#complex-query" aria-hidden="true">#</a> Complex query</h2><p><img src="'+o+'" alt="complex_query"></p><ol><li>In some scenarios, when Grouping is performed and then Top N is obtained, a large amount of data after grouping will be returned. If these data are returned to a computing node at this time, the memory of this node may become a bottleneck. Therefore, Introduced Intermediate Broker nodes to participate in the calculation of intermediate results</li><li>The execution plan will select a specific Intermediate Broker to participate in the calculation according to the number of clusters in the currently available Broker nodes.</li><li>The Root first sends the request to the Intermediate Broker, and after the Intermediate Broker completes the query task, the Root sends the task to the Storage node</li><li>Storage will shard the data to the specified Intermediate Broker node according to the hash value of the Series after grouping, so that the same Series data can be sharded to the same Intermediate Broker for Aggregation operation to improve the locality of data aggregation</li><li>Finally, each Intermediate Broker node returns its own calculation result to the Root node, and the Root merges to generate the final result</li></ol><h2 id="cross-idc-query" tabindex="-1"><a class="header-anchor" href="#cross-idc-query" aria-hidden="true">#</a> Cross-IDC Query</h2><p><img src="'+t+'" alt="cross_idc_query"></p><ul><li>LinDB&#39;s cross-IDC is done at the Query layer, so this kind of query can be understood as a re-aggregation operation after the above two queries are sent to each IDC</li></ul>',22);function c(d,s){return n}var u=r(i,[["render",c],["__file","query.html.vue"]]);export{u as default};
