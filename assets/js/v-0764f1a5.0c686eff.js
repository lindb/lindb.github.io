"use strict";(self.webpackChunklindb_doc=self.webpackChunklindb_doc||[]).push([[626],{1357:(e,o,t)=>{t.r(o),t.d(o,{default:()=>A});var i=t(6252),d=t(3547),a=t(610),c=t(6989);const n=(0,i.uE)('<h1 id="replication" tabindex="-1"><a class="header-anchor" href="#replication" aria-hidden="true">#</a> Replication</h1><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview" aria-hidden="true">#</a> Overview</h2><p>For the replication of distributed storage, consensus protocols such as Paxos, Raft, and ZAB are commonly used in the industry. More than half of these protocols will sacrifice certain performance to strictly ensure data consistency.</p><p>The scenario faced by <code>LinDB</code> is that the amount of writing is huge, and the requirements for data consistency are not high. It also allows short-term data inconsistency and eventually consistent. Therefore, <code>LinDB</code> adopts the strategy that when successfully written to the <code>Leader</code> the write operation is successful.</p><p>Written to <code>Leader</code> is successful has the following benefits:</p><ul><li>Improving write performance;</li><li>Any machine can be deployed in the cluster, while Paxos, Raft, ZAB, etc. require at least 3 machines;</li></ul><p>Written to <code>Leader</code> is successful, has the following problems:</p><ul><li>Data consistency problem: once the <code>Leader</code> is down, a new <code>Leader</code> will be elected, then the new <code>Leader</code> copying new data may overwrite the data written by the old <code>Leader</code>;</li><li>Data loss problem: once <code>Leader</code> is down, and the data block which was not copied to <code>Follower</code> is also damaged at this time, this part of data will be lost;</li></ul><p>In order to solve the above problems and try to ensure that data is not lost, <code>LinDB</code> adopts a multichannel replication scheme, as shown in the following figure:</p><p><img src="'+d+'" alt="replication"></p><p>Take the data shard <code>Shard 3</code> as an example:</p><ul><li>When <code>Node 1</code> is a <code>Leader</code>, <code>1-WAL</code> write channel is enabled, <code>Node 1</code> receives new data and writes it to the <code>1-WAL</code> channel, and replicating the data into <code>1-WAL</code> channel of <code>Node 2</code> and <code>Node 4</code>;</li><li>When <code>Node 1</code> is down and <code>Node 2</code> is elected as the new <code>Leader</code>, the <code>2-WAL</code> write channel is enabled, and <code>Node 2</code> receives new data and replicating the data into <code>2-WAL</code> channel of <code>Node 4</code>;</li><li>When <code>Node 1</code> is up again, <code>Node 1</code> continue to copy the data into <code>1-WAL</code> channel that has not been copied to <code>Node 2</code> and <code>Node 4</code> to their corresponding <code>1-WAL</code> channels . <code>Node 2</code> will supplement the data in the <code>2-WAL</code> channel to the <code>2-WAL</code> channel of <code>Node 1</code>;</li></ul><p>That is, data in write channel flows from <code>Leader</code> . For example, the <code>1-WAL</code> channel is always the <code>Node 1</code> replication channel to <code>Node 2</code> and <code>Node 4</code>, and the <code>2-WAL</code> channel is always <code>Node 2</code> replication channels to <code>Node 1</code> and <code>Node 4</code>, <code>4-WAL</code> channel is always <code>Node 4</code> replication channel to <code>Node 1</code> and <code>Node 2</code>, and so on,</p><div class="custom-container tip"><p class="custom-container-title">TIP</p><p><code>Node 1</code> corresponds to the unique identifier <code>1</code> in the cluster, and so on for other nodes.</p></div><p>This way of replication solves the following problems:</p><ul><li>Avoid data inconsistency: only one node is responsible for the authority of the data for each channel, so there will be no conflict;</li><li>Avoid data loss as much as possible: as long as the data which has not been copied in the old <code>Leader</code> is still persistent at disk, it will be copied to other replicas after it gets up again. If the data that has not been copied in the old <code>Leader</code> is lost, then which will be lost forever;</li></ul><p>Preconditions for multichannel replication:</p><ul><li>Out of order is allowed: data written to multiple channels loses its order. Transactions are not required for Time series data scenarios faced by <code>LinDB</code> so out of order is allowed.</li></ul><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Since a major feature of time series data is time correlation, <code>LinDB</code> also shards data by time according to this feature when storing data. Each time shard is a storage unit, so the actual replication channel is also stored with Units correspond one-to-one.</p></div><h2 id="local-replication" tabindex="-1"><a class="header-anchor" href="#local-replication" aria-hidden="true">#</a> Local replication</h2><p><img src="'+a+'" alt="local replication"></p><p>The entire Local Replication writing process is as follows:</p>',22),l=(0,i._)("li",null,[(0,i.Uk)("The system will start a writing goroutine for each "),(0,i._)("code",null,"Shard"),(0,i.Uk)(" replication channel, this goroutine is responsible for all write operations on this channel, whatever it is a "),(0,i._)("code",null,"Leader"),(0,i.Uk)(" or a "),(0,i._)("code",null,"Follower"),(0,i.Uk)(";")],-1),r=(0,i._)("code",null,"namespace/metric name/tags/fields",-1),s=(0,i._)("code",null,"metric id/time series id/field id",-1),h=(0,i._)("code",null,"string->int",-1),p=(0,i._)("code",null,"namespace/metric name/tags/fields",-1),u=(0,i._)("code",null,"cpu{host= 1.1.1.1} load=1 1514214168614",-1),f=(0,i._)("code",null,"id",-1),w=(0,i._)("code",null,"cpu => 1(metric id), host=1.1.1.1 => 1(time series id), load => 1(field id)",-1),m=(0,i._)("code",null,"1 1 1514214168614 =>1",-1),g=(0,i._)("li",null,[(0,i.Uk)("If writing index operation fails, it is considered that the writing has failed, and the failure is composed of two cases`; "),(0,i._)("ol",null,[(0,i._)("li",null,"Bad data writing format, such failures are directly marked as failures;"),(0,i._)("li",null,"Internal failure, the writing operations will be retried;")])],-1),b=(0,i._)("code",null,"ID",-1),y=(0,i._)("code",null,"LSM",-1),L=(0,i._)("code",null,"Flush",-1),k=(0,i.uE)('<p>Here are two import replication <code>Index</code> for write operations:</p><ul><li><code>Consume(Replica) Index</code>: where the writing goroutine has been processed, each write operation will first verify whether the <code>Index</code> is legal;</li><li><code>Ack Index</code>: which data in this channel has been successfully written and persisted to storage;</li></ul><p>Here are a few things to note:</p><ol><li>The writing goroutine will consume the written data from the <code>WAL</code> channel in order, so the <code>Replica Index</code> is an ordered pointer, so it is easy to check with the persisted <code>Index</code> to verify the data whether it has been written;</li><li>The <code>Flush</code> goroutine is used to synchronize the <code>Ack Index</code> to notify which data has been written successfully;</li><li>Since all write operations write into memory firstly, and then persist the data into corresponding file. If the system crashes during this process, as there is no <code>Ack Index</code>, even if the data in the memory is lost, but when the goroutine starts again, it consumes the data in the <code>WAL</code> channel with <code>Ack Index</code> as the current <code>Replica Index</code>. This consuming process achieved that data can be recovered after crashing;</li></ol><h2 id="remote-replication" tabindex="-1"><a class="header-anchor" href="#remote-replication" aria-hidden="true">#</a> Remote replication</h2><p>Pick <code>Node 1</code> as <code>Leader</code> in the above figure with 3 Replications replicating data from <code>1-WAL</code> as an example:</p><p><code>Node 1</code> acts a <code>Leader</code> of the shard to accept written data from <code>Broker</code>, <code>Node 2</code> and <code>Node 4</code> are both <code>Follower</code> accepting the replication request of <code>Node 1</code>, meanwhile the <code>1-WAL</code> channel is used as data write channel.</p><p><img src="'+c+'" alt="remote replication"></p><p><code>Index</code> basic concept description:</p><ul><li><code>Append Index</code> of each channel, indicating the writing position of the channel ;</li><li>Each channel retains each <code>Follower Consumer (Replica) Index</code> and <code>Ack Index</code>, respectively indicating the position where each <code>Follower</code> is consumed (consumed but the request is still without successful consumption acknowledged) and position of successful consumption;</li><li><code>Tail Index</code> of each channel: indicates the minimum value of all <code>Follower Ack Index</code>, <code>WAL</code> before the <code>Index</code> is deletable ;</li></ul><p>The entire replication process is as follows:</p><ul><li><code>Leader</code> will start an independent goroutine for each <code>Follower</code> replication, get <code>Follower Consumer(Replica) Index</code> from <code>WAL</code> of <code>Leader</code> then send it to <code>Follower</code>;</li><li><code>Follower</code> compares the <code>Append Index</code> with <code>Consumer(Replica) Index</code> in local <code>WAL</code>; <ul><li>On equal: return <code>Consumer Index</code> to <code>Leader</code> as <code>Ack Index</code> (normal case);</li><li>On not equal, return the <code>Append Index</code> in its own local <code>WAL</code> to <code>Leader</code>;</li></ul></li><li>After <code>Leader</code> receiving response, detect whether the <code>Ack Index</code> of<code>Follower</code> is equal to <code>Consumer(Replica) Index</code>; <ul><li>On equal: update the <code>Ack Index</code> of the <code>Follower</code> (normal case);</li><li>On not equal: here are some cases: <ul><li>On <code>Follower Ack Index</code> smaller than <code>Tail Index</code> of <code>Leader WAL</code>: this means that <code>Follower</code> local <code>Append Index</code> is too small while <code>Leader</code> has no data at that position, then <code>Follower</code> should reset its own <code>Append Index</code> as <code>Tail Index</code> of <code>Leader</code>;</li><li>On <code>Follower Ack Index</code> is greater than the <code>Append Index</code> of the <code>Leader WAL</code>: this means that the <code>Append Index</code> of the <code>Follower</code> is too large, and the <code>Leader</code> has no data at this position, and the <code>Leader WAL</code> needs to be changed. Append Index<code>is raised to</code>Follower Ack Index<code>, and </code>Follower Consumer(Replica) Index<code>is raised to</code>Follower Ack Index`;</li><li>On <code>Follower</code> is between <code>Tail Index</code> and <code>Append Index</code> of <code>Leader WAL</code>: <code>Follower Consumer(Replica) Index</code> will be reset to <code>Follower Ack Index</code>;</li></ul></li></ul></li></ul><p>The process of <code>Leader</code> and <code>Follower</code> to initialize the replication channel is similar to <code>TCP</code> three-way handshake.</p><h2 id="sequentiality" tabindex="-1"><a class="header-anchor" href="#sequentiality" aria-hidden="true">#</a> Sequentiality</h2><p>Generally speaking, multichannel replication does not guarantee the order of the overall data. In most cases, only one of the channels is used. It is necessary to ensure the order of replication of the channel. With sequentiality, it is easier to know which data has been copied. Which data has not been replicated.</p><p>To ensure the sequentiality of replication process, it is necessary to ensure the order of the following steps:</p><ul><li><code>Leader</code> send replication requests in order;</li><li><code>Follower</code> processes replication request in order;</li><li><code>Leader</code> responses to replication request in order;</li></ul><p>Since the entire writing and replication process is based on the <code>GRPC Stream</code> long connection and single-goroutine processing mechanism, the above conditions can basically be guaranteed.</p><h4 id="reference" tabindex="-1"><a class="header-anchor" href="#reference" aria-hidden="true">#</a> Reference</h4>',19),x={href:"https://github.com/bulldog2011/bigqueue",target:"_blank",rel:"noopener noreferrer"},I={},A=(0,t(3744).Z)(I,[["render",function(e,o){const t=(0,i.up)("RouterLink"),d=(0,i.up)("ExternalLinkIcon");return(0,i.wg)(),(0,i.iD)("div",null,[n,(0,i._)("ul",null,[l,(0,i._)("li",null,[(0,i.Uk)("First, data corresponding to "),r,(0,i.Uk)(" into the index file of the database will be written, and the corresponding "),s,(0,i.Uk)(" will be generated, this operation is mainly about completing the conversion of "),h,(0,i.Uk)("; Advantage of this conversion is that all data are stored in data types to reduce the overall storage size, because for each data point, metadata such as "),p,(0,i.Uk)(" is occupied by such as "),u,(0,i.Uk)(", In fact, after converting to "),f,(0,i.Uk)(", the storage will be "),w,(0,i.Uk)(", simplified to "),m,(0,i.Uk)(", please refer to "),(0,i.Wm)(t,{to:"/design/index_.html"},{default:(0,i.w5)((()=>[(0,i.Uk)("index")])),_:1}),(0,i.Uk)(" for the specific index structure;")]),g,(0,i._)("li",null,[(0,i.Uk)("When obtained "),b,(0,i.Uk)(", data will be written into storage uint, this process is similar to "),y,(0,i.Uk)(", which writes data into the memory database firstly, and directly write to the memory to achieve high throughput performance. After the size of memory data reaches the memory limit, the "),L,(0,i.Uk)(" operation will be triggered. For details, please refer to "),(0,i.Wm)(t,{to:"/design/storage.html"},{default:(0,i.w5)((()=>[(0,i.Uk)("Storage Format")])),_:1}),(0,i.Uk)(", "),(0,i.Wm)(t,{to:"/design/memory.html"},{default:(0,i.w5)((()=>[(0,i.Uk)("Memory Database")])),_:1}),(0,i.Uk)(";")])]),k,(0,i._)("ol",null,[(0,i._)("li",null,[(0,i._)("a",x,[(0,i.Uk)("bigqueue"),(0,i.Wm)(d)])])])])}]])},610:(e,o,t)=>{e.exports=t.p+"assets/img/local_replication.b17993af.png"},6989:(e,o,t)=>{e.exports=t.p+"assets/img/remote_replication.1f2f1255.png"},3547:(e,o,t)=>{e.exports=t.p+"assets/img/storage_database.1c521be9.png"},2147:(e,o,t)=>{t.r(o),t.d(o,{data:()=>i});const i=JSON.parse('{"key":"v-0764f1a5","path":"/design/replication.html","title":"Replication","lang":"en-US","frontmatter":{},"excerpt":"","headers":[{"level":2,"title":"Overview","slug":"overview","link":"#overview","children":[]},{"level":2,"title":"Local replication","slug":"local-replication","link":"#local-replication","children":[]},{"level":2,"title":"Remote replication","slug":"remote-replication","link":"#remote-replication","children":[]},{"level":2,"title":"Sequentiality","slug":"sequentiality","link":"#sequentiality","children":[{"level":4,"title":"Reference","slug":"reference","link":"#reference","children":[]}]}],"git":{"updatedTime":1681286958000,"contributors":[{"name":"stone1100","email":"stonejh1100@gmail.com","commits":1}]},"filePathRelative":"design/replication.md"}')}}]);